spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trade_db_dev?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  
  data:
    redis:
      host: localhost
      password: 

logging:
  level:
    root: INFO
    com.example.trade: DEBUG
    org.springframework.ai: DEBUG
    com.baomidou.mybatisplus: DEBUG

# 开发环境关闭缓存
spring.cache.type: none